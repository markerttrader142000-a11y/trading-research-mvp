pydantic>=2.7,<3
PyYAML>=6,<7
langgraph>=0.2,<0.3
eval-type-backport>=0.2,<1
requests>=2.31,<3
ctrader-open-api>=0.9,<1
service-identity>=24.1.0

# CrewAI — Layer 3: Analysis Workflows
crewai>=0.80,<1
crewai-tools>=0.14,<1

# LLM providers (install only when API keys are available)
# anthropic>=0.30,<1          # Claude
# openai>=1.30,<2             # ChatGPT / GPT-4o
litellm>=1.40,<2              # Mistral, Perplexity, Gemini, Grok, DeepSeek via LiteLLM
